# Copy to .env.local for local dev, and add the same key in Vercel
# (Project → Settings → Environment Variables).
#
# Get a free access key by entering firstlineholidys@gmail.com at
# https://web3forms.com — every form submission is emailed to that inbox.
WEB3FORMS_ACCESS_KEY=
